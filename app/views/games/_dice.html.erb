<% @dice.each do |dice| %>
  <%= link_to (image_tag dice[:image]), { controller: :games, action: :roll }, method: :get, remote: true, id: 'roll_button', class: (dice[:canFreeze] ? 'activeHighlight' : 'inactiveHighlight') %>
<% end %>
