<%= form_for(@game_state) do |f| %>
  <% if @game_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_state.errors.count, "error") %> prohibited this game_state from being saved:</h2>

      <ul>
      <% @game_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
