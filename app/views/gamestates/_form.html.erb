<%= form_for(@gamestate) do |f| %>
  <% if @gamestate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gamestate.errors.count, "error") %> prohibited this gamestate from being saved:</h2>

      <ul>
      <% @gamestate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :phase_id %><br>
    <%= f.text_field :phase_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
