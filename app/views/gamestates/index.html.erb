<p id="notice"><%= notice %></p>

<h1>Listing Gamestates</h1>

<table>
  <thead>
    <tr>
      <th>Game</th>
      <th>Phase</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gamestates.each do |gamestate| %>
      <tr>
        <td><%= gamestate.game %></td>
        <td><%= gamestate.phase %></td>
        <td><%= link_to 'Show', gamestate %></td>
        <td><%= link_to 'Edit', edit_gamestate_path(gamestate) %></td>
        <td><%= link_to 'Destroy', gamestate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Gamestate', new_gamestate_path %>
