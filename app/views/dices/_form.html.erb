<%= form_for(@dice) do |f| %>
  <% if @dice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dice.errors.count, "error") %> prohibited this dice from being saved:</h2>

      <ul>
      <% @dice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
