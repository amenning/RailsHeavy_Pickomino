<%= form_for(@player_option) do |f| %>
  <% if @player_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_option.errors.count, "error") %> prohibited this player_option from being saved:</h2>

      <ul>
      <% @player_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_state_id %><br>
    <%= f.text_field :game_state_id %>
  </div>
  <div class="field">
    <%= f.label :can_roll %><br>
    <%= f.number_field :can_roll %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
