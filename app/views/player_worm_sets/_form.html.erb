<%= form_for(@player_worm_set) do |f| %>
  <% if @player_worm_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_worm_set.errors.count, "error") %> prohibited this player_worm_set from being saved:</h2>

      <ul>
      <% @player_worm_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.text_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
